TO Execute:
1.. sudo mn -c
2.. sudo python custom_topology.py
3.. then enter number of switches required
4.. then enter number of hosts required 

output :
 Shows result of pingall command
 shows bandwidth between h3 and h1 if number of hosts are grater than 2
 shows bandwidth between h3 and h1 if number of hosts are grater than 3

